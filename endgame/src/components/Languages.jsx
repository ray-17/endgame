import { languages } from "../languages"


export default function Languages(props){

    

    return(
        <section className="chips">
        <li>{props.name}</li>        
        </section>
    )

}